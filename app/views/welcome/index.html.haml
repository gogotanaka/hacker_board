#welcome
  = link_to new_news_path, class: 'btn btn-danger' do
    %i.fa.fa-pencil
    ニュースを投稿する

  = link_to new_user_registration_path, class: 'btn btn-success' do
    %i.fa.fa-pencil
    ユーザー登録
  %ul#newsWrapper
    - News.all.each.with_index(1) do |news, i|
      %li
        %p.rankingIcon= i
        %h2= news.title
        = news.contents


